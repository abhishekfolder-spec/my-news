feedparser>=6.0
PyYAML>=6.0
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
trafilatura>=1.8      # full-text extraction (ET / Moneycontrol enrichment)
anthropic>=0.40       # only used if AI summary is enabled
